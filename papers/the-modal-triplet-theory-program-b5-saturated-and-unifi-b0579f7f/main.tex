\documentclass[11pt]{article}
\usepackage{series}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{geometry}
\usepackage{enumitem}
\usepackage{array}

\geometry{margin=1in}

\newcommand{\Contract}{\mathfrak C}
\newcommand{\Mod}{\mathcal M}
\newcommand{\Sol}{\mathcal Z}
\newcommand{\Cat}{\mathcal C}
\newcommand{\Blocks}{\mathcal B}
\newcommand{\Decomp}{\mathcal D}
\newcommand{\Anom}{\mathcal A}
\newcommand{\Mechanisms}{\mathcal K}
\newcommand{\id}{\operatorname{id}}

\title{The Modal Triplet Theory Program B5:\\
Relative Saturation, Conditional Extended Carriers,\\
and String-Like Realizations}
\author{Peter Nero}
\date{September 2026\\Version 5}

\begin{document}

\maketitle

\begin{abstract}
We replace an absolute notion of ``encoding saturation'' by a typed and
testable relative notion.  A saturation contract specifies a category of
candidate realizations, its equivalences, a complete list of constraints and
boundary data, and a class of allowed factorizations.  A realization is
saturated relative to that contract when it satisfies every declared
constraint and admits no nontrivial allowed factorization.  Saturation can
therefore change when the candidate class, constraint inventory, equivalence,
or factorization class changes.

For a finite-dimensional local model with declared variable and constraint
blocks, we prove that first-order factorization is exactly detected by the
connected components of the bipartite derivative-incidence graph.  A connected
active graph gives infinitesimal indecomposability; together with an injective
combined derivative, it gives a locally isolated, infinitesimally
indecomposable realization.  This is a genuine conditional saturation theorem.

The stronger claims in the first version do not follow from saturation alone.
We give explicit countermodels showing that a complete, rigid, indecomposable
constraint system need not contain extended carriers, a critical dimension, or
a nontrivial duality.  A one-dimensional carrier is minimal only after one
requires a nonconstant continuous loop or nontrivial holonomy.  Critical
dimensions require an explicit anomaly or central-charge defect.  Duality
requires an explicit invertible comparison preserving the declared dynamics
and observables.  String-like and brane-like structures are consequently
realization classes, not inevitable consequences of three obstruction labels.

We also separate existence, local rigidity, uniqueness within a declared
candidate class, physical selection, and empirical adequacy.  The current
selected q=79 arithmetic theorem supplies an exact finite branch, but the
physical visible-hidden Hull--Strominger endpoints, the remaining seven rows of
the twelve-row worldsheet contract, and an all-scale nonperturbative completion
remain open.  Program B5 therefore provides a rigorous language and a finite
indecomposability test for unified encodings.  It does not derive string theory,
a numerical critical dimension, or the physical selection of a saturated
universe.
\end{abstract}

\section*{Version 5 Revision Note}
\noindent\textbf{Supersedes.} Version 4, which remains the released edition.
\textbf{Reason and resolution.} Distinguishes the local Circle--Lens--Nil carrier taxonomy from the global q79 geometry.
\textbf{Retained results.} The previous mathematical results and proofs are unchanged.
\textbf{Boundary.} This is a contextual clarification in an unreleased revision,
not a new physical-source theorem.

\section*{Revision note for version 4}
\addcontentsline{toc}{section}{Revision note for version 4}

\begin{description}[leftmargin=2.5cm,style=nextline]
\item[Supersedes.]
Version~3 of Program B5.

\item[Reason.]
Version~3 already supplied the reviewed relative-saturation theorem,
countermodels, and explanatory wiring-diagram presentation.  Its managed
reproducibility block nevertheless reported that no result row was mapped,
although the paper uses the exact selected q=79 arithmetic packet.

\item[Resolution.]
Version~4 maps the A11 q=79 theorem explicitly in both canonical sources and
pins the curated repository revision.  The in-paper release note now records
only the current version delta; the complete revision history remains in the
accompanying audit.  No theorem or physical-selection claim changes.

\item[Retained content.]
The relative saturation contract, derivative-incidence criterion,
countermodels, and conditional status of extended carriers, dimensions,
anomalies, and dualities are unchanged.

\item[Open boundary.]
An exhaustive realization category, physical string background, completed
q79 worldsheet, nonperturbative completion, and physical selection theorem
remain open.
\end{description}

\begingroup
\footnotesize
\setlength{\parskip}{0pt}
\tableofcontents
\endgroup
\newpage

\section{How to Read Program B5}

Program B5 asks whether a successful collection of constraints is genuinely
one coupled construction or merely several independent theories written next
to one another.  The paper calls the first situation \emph{saturated}, but
only relative to a declared contract.  This qualification is essential:
changing which variables are primitive, which constraints count, or which
factorizations are allowed can change the answer.

\subsection{The central picture: a wiring diagram}

At a solution, linearize every declared constraint.  Place the variable blocks
on one side of a bipartite graph and the constraint blocks on the other.  Draw
an edge whenever changing a variable block changes a constraint block to
first order.  If this graph falls into two pieces, the linearized problem also
falls into two independent subsystems.  If it is connected, every variable
block is tied into one first-order network.

This wiring picture gives the paper's exact finite test.  It does not say that
the full nonlinear theory cannot be separated by a clever change of
variables, nor that the solution is globally unique.  Those stronger claims
need their own certificates.  The injective-derivative condition supplies one
additional fact: the same solution is locally isolated.

\subsection{Five questions kept separate}

The argument proceeds through five questions.
\begin{enumerate}
\item Is the candidate complete with respect to the displayed constraint
inventory?
\item Is the displayed system indecomposable with respect to the allowed
factorizations?
\item Does a separate carrier requirement force positive dimension or a
worldsheet-like sweep?
\item Do explicit anomaly, criticality, and duality data make that sweep a
string-like physical realization?
\item Is one realization selected by a source law and empirically adequate?
\end{enumerate}
The derivative-incidence theorem answers the second question locally.  The
loop theorem answers a narrow version of the third.  The anomaly, duality, and
string packages state what must be added for the fourth.  Nothing in the word
``saturation'' answers the fifth.

\subsection{Suggested reading routes}

Readers interested in the exact mathematical contribution can move from the
contract definitions to the derivative-incidence theorem and its finite
example.  Readers interested in the string interpretation should then read
the countermodels before the worldsheet, anomaly, and duality sections; those
countermodels prevent conditional realization mechanisms from being mistaken
for consequences of saturation.  The final theorem ledger and reusable audit
collect the result and every remaining boundary.

\section{Scope and Dependencies}

Program B5 begins after the typed intersection theory of Program B4.  It does
not infer a unified theory directly from the words circle, lens, and nil.
Program A0 supplies typed reductions, quotient relations, and factorization
criteria~\cite{A0}.  Program B0 treats circle, lens, and nil as useful but
nonexhaustive obstruction profiles~\cite{B0}.  Programs B1--B3 state the
additional geometric, gauge, and quantum data needed before those profiles
become physical theories~\cite{B1,B2,B3}.  Program B4 defines a common
realization locus and separates compatibility, local rigidity, persistence,
and global uniqueness~\cite{B4}.

These Circle--Lens--Nil profiles are not a global topology and are not
identified with the selected q79 Fu--Yau/Hull--Strominger compactification.
They remain a local obstruction and carrier taxonomy unless an explicit
global realization map is supplied.

The question addressed here is narrower:
\begin{quote}
Given a fully declared intersection contract, when do its constraint blocks
fail to split into independent subsystems, and what additional assumptions are
needed before that indecomposability can be realized by strings, worldsheets,
critical dimensions, anomalies, or dualities?
\end{quote}

\subsection{Terminology boundary}

The word \emph{saturation} has several standard mathematical meanings.  Ideal
saturation $I:f^\infty$, saturation of a sublattice, a saturated subsheaf, and
the contract-relative notion introduced below are different constructions.
An exact computation involving one of the first three does not prove
descriptive saturation unless a map between the two notions is supplied.

\subsection{Current q=79 evidence}

One later selected result is relevant as a boundary marker.  The exact q=79
packet proves, on its stated finite branch, that
\[
 q\equiv 15\pmod {64},
 \qquad
 q\equiv 2\pmod 7,
 \qquad
 q\equiv 79\pmod {448}.
\]
This is an exact finite arithmetic result~\cite{Q79}.  The integer $79$ is not
a spacetime dimension, a worldsheet central charge, or a proof of string
criticality.

The current physical string branch remains conditional:
\begin{enumerate}
\item the selected visible and hidden non-pullback holomorphic bundles and
their common positive HYM chamber are not yet constructed;
\item the q=79 worldsheet contract is complete in five of twelve declared rows;
and
\item all-genus or other nonperturbative completion, positivity, and
asymptotic control remain open.
\end{enumerate}
Those open rows are not assumptions silently imported into the theorems below.

\section{A Relative Saturation Contract}

\subsection{Typed data}

\begin{definition}[Saturation contract]\label{def:contract}
A \emph{saturation contract} is a tuple
\[
 \Contract
 =
 \bigl(
 \Cat,\sim,\Mod,\{F_\alpha\}_{\alpha\in A},
 \Blocks,\Decomp,\mathcal R
 \bigr)
\]
with the following data:
\begin{enumerate}
\item a category or structured class $\Cat$ of candidate realizations;
\item an equivalence relation or groupoid $\sim$ of admissible
re-encodings;
\item a moduli object $\Mod$, or a local slice after quotienting the declared
equivalences;
\item a finite or otherwise controlled inventory of constraint maps
$F_\alpha:\Mod\to W_\alpha$;
\item declared variable and constraint blocks $\Blocks$;
\item a class $\Decomp$ of factorizations that count as a genuine separation
of responsibilities; and
\item regularity, source, boundary, and domain data $\mathcal R$.
\end{enumerate}
\end{definition}

The index set $A$ may contain geometric transport, bundle cocycle, gauge,
quantum, anomaly, overlap, boundary, and refinement constraints.  The labels
are bookkeeping devices.  Their equations need not be independent.

\begin{definition}[Contract-complete realization]
The common realization locus is
\[
 \Sol_{\Contract}
 =
 \left\{
 [x]\in\Mod/{\sim}:
 F_\alpha(x)=0\ \text{for every }\alpha\in A,
 \ \text{and }\mathcal R\text{ holds}
 \right\}.
\]
A point $[x]\in\Sol_{\Contract}$ is \emph{contract complete}.
\end{definition}

Completeness is relative to the declared inventory.  The phrase ``all
constraints'' means every row in $A$, not every obstruction that could be
invented in a larger theory.

\subsection{Allowed factorization}

\begin{definition}[Local contract factorization]\label{def:factorization}
Let $[x]\in\Sol_{\Contract}$.  A nontrivial local factorization in the class
$\Decomp$ consists of:
\begin{enumerate}
\item a neighborhood $U$ of $[x]$ and an allowed equivalence
$U\simeq U_1\times U_2$, with both factors nontrivial;
\item a partition $A=A_1\sqcup A_2$; and
\item maps $\widetilde F_\alpha$ such that, under the equivalence,
\[
 F_\alpha=
 \begin{cases}
 \widetilde F_\alpha\circ\pi_1,&\alpha\in A_1,\\
 \widetilde F_\alpha\circ\pi_2,&\alpha\in A_2.
 \end{cases}
\]
\end{enumerate}
Boundary data, sources, and equivalences must factor in the same declared
sense.
\end{definition}

\begin{definition}[Relative saturation]\label{def:saturation}
A contract-complete realization is \emph{$\Decomp$-saturated} when it admits no
nontrivial local factorization from Definition~\ref{def:factorization}.
It is \emph{globally $\Decomp$-saturated} when no such factorization exists on
its full connected component.
\end{definition}

This definition says what is inseparable and which splittings were tested.  It
does not build a carrier, action, anomaly theory, or physical selector.

\begin{proposition}[Saturation is contract relative]\label{prop:relative}
Relative saturation is not an intrinsic predicate of an untyped object.
Changing the constraint inventory, candidate category, equivalence, block
decomposition, or allowed factorization class can change its truth value.
\end{proposition}

\begin{proof}
Adding a constraint can remove a point from $\Sol_{\Contract}$.  Enlarging
$\Decomp$ can admit a previously forbidden factorization.  Refining a variable
block can expose a splitting hidden by a coarse block, while quotienting by a
larger equivalence can identify factors that were previously distinct.
Therefore the predicate depends on the displayed contract data.
\end{proof}

\section{The Derivative-Incidence Criterion}

\subsection{Blocked linearization}

Let $\Mod$ be a smooth finite-dimensional local slice near a
contract-complete point $x$.  Write
\[
 V=T_x\Mod=\bigoplus_{i\in I}V_i,
 \qquad
 W=\bigoplus_{j\in J}W_j,
 \qquad
 L=DF_x:V\longrightarrow W,
\]
where $F=(F_j)_{j\in J}$.  Discard constraint blocks with zero derivative.
If a variable block has no incident derivative, it is a free first-order
factor and is retained as an isolated vertex.

\begin{definition}[Derivative-incidence graph]
The bipartite graph $G_x$ has variable vertices $I$, constraint vertices $J$,
and an edge $i\!-\!j$ exactly when
\[
 L_j|_{V_i}\ne 0.
\]
\end{definition}

\begin{definition}[First-order block factorization]
A \emph{first-order block factorization} is a pair of nonempty partitions
\[
 I=I_1\sqcup I_2,
 \qquad
 J=J_1\sqcup J_2
\]
such that
\[
 L_j|_{V_i}=0
 \quad\text{whenever}\quad
 (i,j)\in(I_1\times J_2)\cup(I_2\times J_1).
\]
\end{definition}

\begin{theorem}[Incidence criterion for infinitesimal
indecomposability]\label{thm:incidence}
After deleting zero constraint blocks, $L$ has a nontrivial first-order block
factorization if and only if the derivative-incidence graph $G_x$ is
disconnected with a partition containing variable vertices on both sides.
Consequently, a connected $G_x$ proves first-order indecomposability relative
to the declared blocks.
\end{theorem}

\begin{proof}
Suppose the displayed partitions give a first-order factorization.  Every
nonzero derivative entry joins $I_1$ only to $J_1$ or $I_2$ only to $J_2$.
There is no edge crossing between the two vertex sets, so $G_x$ is
disconnected.

Conversely, let $G_x=G_1\sqcup G_2$ be a disconnection with variable vertices
in both components.  Put $I_k=I\cap G_k$ and $J_k=J\cap G_k$.  No edge joins
$I_1$ to $J_2$ or $I_2$ to $J_1$, which is exactly the vanishing condition for
a first-order block factorization.  More than two components can be grouped
into two nonempty collections.
\end{proof}

\begin{corollary}[Locally rigid infinitesimal
saturation]\label{cor:rigid-saturation}
Assume $F(x)=0$, $G_x$ is connected, and $DF_x$ is injective.  Then $x$ is
locally isolated in $F^{-1}(0)$ and first-order indecomposable relative to the
declared blocks.
\end{corollary}

\begin{proof}
Theorem~\ref{thm:incidence} gives first-order indecomposability.  If
$n=\dim V$, injectivity supplies an $n\times n$ nonzero minor after choosing
$n$ target coordinates.  The inverse function theorem applied to those
components makes their common zero locally unique.  Hence the full zero set is
locally $\{x\}$.
\end{proof}

\begin{remark}[Exact scope]
Connected incidence is a statement about the displayed linearized blocks.
It does not exclude a nonlinear change of variables that factors the contract,
nor does it establish global uniqueness.  Injectivity is sufficient for local
isolation, not necessary: singular equations can also have isolated zeros.
\end{remark}

\subsection{A finite exact example}

Let $\Mod=\mathbb R^3$ with scalar variable blocks and define
\[
 F(x_1,x_2,x_3)
 =
 \begin{pmatrix}
 x_1+x_2\\
 x_2+x_3\\
 x_3+x_1
 \end{pmatrix}.
\]
The derivative matrix is
\[
 L=
 \begin{pmatrix}
 1&1&0\\
 0&1&1\\
 1&0&1
 \end{pmatrix},
 \qquad
 \det L=2.
\]
Its incidence graph is a six-cycle, hence connected, and $L$ is invertible.
The origin is therefore locally rigid and first-order indecomposable.

This example is deliberately point-valued.  It contains no extended carrier,
worldsheet, critical dimension, anomaly polynomial, or duality.  It proves
that even the conjunction of completeness, rigidity, and first-order
indecomposability does not imply any of those additional structures.

The matrix also makes the two tests visibly different.  Its nonzero pattern
connects all six graph vertices, which certifies coupling.  Its determinant
certifies local isolation.  A connected singular matrix could pass the first
test and fail the second, while an invertible block-diagonal matrix could be
locally isolated yet split into independent sectors.  Saturation and rigidity
therefore measure different properties.

\section{What Saturation Does Not Force}

\subsection{Pointlike data remain possible}

A pointwise field is not the same as a collection of independent points.
Sections of a bundle are evaluated pointwise while their derivatives,
connections, boundary conditions, and gauge transformations couple values
across the base.  Conversely, a finite-dimensional or zero-dimensional model
can have a connected constraint graph, as the preceding example shows.

Therefore the old implication
\[
 \text{saturation}\Longrightarrow
 \text{failure of all pointlike encodings}
\]
is false without a separate definition of pointlike locality and a theorem
showing that every allowed local model violates a named constraint.

\subsection{The conditional carrier-minimality theorem}

There is a valid narrower statement behind the earlier intuition.

\begin{theorem}[Minimal dimension for continuous loop
support]\label{thm:loop-dimension}
Let $K$ be a compact metric carrier required to contain a nonconstant
continuous loop.  Then its covering dimension satisfies
\[
 \dim K\ge 1.
\]
The bound is sharp because $S^1$ has covering dimension one and supports a
non-nullhomotopic loop.
\end{theorem}

\begin{proof}
A compact metric space of covering dimension zero is totally disconnected.
The image of $S^1$ under a continuous map is connected.  If the map is
nonconstant, its image contains more than one point, contradicting total
disconnectedness.  The circle supplies the sharp example.
\end{proof}

\begin{remark}
The theorem is conditional on continuous loop support.  An interval is
one-dimensional but has trivial fundamental group.  A circle supports
holonomy but does not by itself supply a gauge group, a nil/refinement law, a
worldsheet action, a quantum measure, or anomaly cancellation.
\end{remark}

\subsection{Worldsheet-like sweeps}

Suppose a one-dimensional carrier $K$ is propagated through an additional
parameter $t\in I$ by a sufficiently regular family of maps
$\gamma_t:K\to X$.  The evaluation map
\[
 \Gamma:K\times I\longrightarrow X,
 \qquad
 \Gamma(k,t)=\gamma_t(k),
\]
has a two-dimensional parameter domain when $K$ is one-dimensional.  This is
a \emph{worldsheet-like sweep}.  Calling it a physical worldsheet additionally
requires at least:
\begin{enumerate}
\item a target geometry and field space;
\item an action and boundary conditions;
\item gauge fixing and a quantum measure or operator construction;
\item local and global anomaly control; and
\item observables and a physical interpretation.
\end{enumerate}
The product $K\times I$ alone proves none of these rows.

\section{Anomaly Completeness and Critical Dimensions}

\subsection{The anomaly inventory must be declared}

\begin{definition}[Anomaly contract]\label{def:anomaly-contract}
An \emph{anomaly contract} consists of:
\begin{enumerate}
\item a field and symmetry content;
\item a quantum construction and regularization domain;
\item a list $\Anom=\{\alpha_r\}_{r\in R}$ of local, global, mixed,
gravitational, or worldsheet anomaly classes that are defined for that model;
\item the cohomology, determinant-line, cobordism, BRST, or Ward-identity
groups in which those classes live; and
\item an allowed mechanism class $\Mechanisms$ of counterterms, inflow,
Green--Schwarz terms, or additional sectors.
\end{enumerate}
\end{definition}

\begin{definition}[Anomaly complete]
A model is \emph{$(\Anom,\Mechanisms)$-anomaly complete} when every declared
class is zero or is trivialized by a displayed mechanism in $\Mechanisms$,
with the compatibility equations checked.
\end{definition}

The phrase ``all potential anomalies cancel'' has no invariant meaning without
Definition~\ref{def:anomaly-contract}.  Classical failure of a bundle cocycle
is also not automatically a quantum anomaly.

In established string constructions, criticality and anomaly cancellation are
obtained from explicit worldsheet central-charge, gauge, gravitational, and
inflow equations~\cite{Polchinski,AlvarezGaumeWitten,GreenSchwarz}.  Their
existence supports string-like realizations of a sufficiently rich contract.
It does not show that abstract indecomposability creates those equations.

\subsection{A conditional criticality theorem}

\begin{theorem}[Analytic anomaly-zero criterion]\label{thm:critical}
Fix all realization data except a real dimension parameter $d$ in an open
interval $U$.  Suppose the net anomaly or central-charge defect
\[
 a:U\longrightarrow\mathbb R
\]
is real analytic and not identically zero.  Then the admissible set
\[
 \{d\in U:a(d)=0\}
\]
is discrete.  It is finite on every compact subinterval whose neighborhood is
contained in $U$.
\end{theorem}

\begin{proof}
Zeros of a nonzero real-analytic function are isolated.  An infinite set of
zeros in such a compact subinterval would have an accumulation point, forcing
$a$ to vanish identically by the identity theorem, a contradiction
~\cite{KrantzParks}.
\end{proof}

\begin{remark}[Why the hypothesis matters]
If $a\equiv0$, every $d$ is allowed.  If
$a(d)=(d-10)(d-26)$, two values survive.  If
$a(d)=\sin(\pi d)$, infinitely many isolated values survive.  The word
``anomaly'' alone selects none of these functions.  Moreover, ordinary
spacetime dimension is already integer-valued; merely observing discreteness
does not derive a critical dimension.
\end{remark}

For several continuous moduli, one equation generally leaves a
positive-dimensional zero locus.  A discrete solution then requires the
appropriate rank, transversality, compactness, or arithmetic hypotheses, as
in Program B4.

\section{Duality Requires a Certificate}

\subsection{Typed duality data}

\begin{definition}[Duality certificate]\label{def:duality}
A duality between two realization descriptions $X$ and $Y$ consists of:
\begin{enumerate}
\item typed source and target state or configuration spaces;
\item maps or functors $D:X\to Y$ and $E:Y\to X$;
\item inverse or natural-equivalence identities on the declared domains;
\item preservation of actions, equations, symplectic or operator structures,
and boundary conditions as applicable;
\item an observable correspondence, including normalization and uncertainty;
and
\item matching anomaly and quantum-consistency data.
\end{enumerate}
\end{definition}

Different theories use different subsets of this list, but an identification
cannot be inferred merely because two descriptions address similar
obstructions.  Standard examples such as target-space T-duality are supported
by explicit transformations and quantum consistency checks
~\cite{Buscher,Polchinski}.

\begin{proposition}[Quotienting does not manufacture
duality]\label{prop:duality}
If a certified duality is included in the equivalence relation $\sim$, then
the quotient $\Mod/{\sim}$ identifies the dual descriptions.  The existence of
a saturated point in $\Mod/{\sim}$ does not, conversely, construct a duality
certificate.
\end{proposition}

\begin{proof}
The first statement is the definition of a quotient by a declared
equivalence.  For the converse, take a contract whose moduli space is one
point and whose category has only its identity morphism.  The point is
contract complete and admits no nontrivial factorization, but there are no two
distinct descriptions and no nontrivial duality.  Alternatively, two isolated
saturated points can be placed in a discrete category with no morphism between
them.
\end{proof}

Thus duality webs are possible and important features of particular saturated
realizations.  They are not forced by relative saturation itself.

\section{String-Like Realizations}

\subsection{Realization package}

\begin{definition}[String-like realization package]\label{def:string-package}
A \emph{string-like realization package} for a saturation contract contains:
\begin{enumerate}
\item a one-dimensional carrier $K$, its allowed topologies and labels;
\item a worldsheet or swept domain $\Sigma$ and target data $X$;
\item a field/configuration space and an action $S$ with boundary conditions;
\item a gauge and quantum construction, including its state or measure data;
\item a declared anomaly contract and explicit trivializations;
\item overlap, refinement, and compactification maps;
\item every claimed duality certificate; and
\item a map from this package into the variables and constraints of
$\Contract$.
\end{enumerate}
\end{definition}

\begin{proposition}[Conditional realization criterion]\label{prop:string}
If the map in Definition~\ref{def:string-package} is type preserving, sends
every declared equation and boundary condition of the package to the
corresponding row of $\Contract$, and its image is contract complete and
$\Decomp$-indecomposable, then the package realizes a saturated encoding.
\end{proposition}

\begin{proof}
Type preservation makes the image a candidate in $\Cat$.  Vanishing of every
constraint and satisfaction of the boundary data place it in
$\Sol_{\Contract}$.  The assumed absence of an allowed factorization is exactly
Definition~\ref{def:saturation}.
\end{proof}

The proposition is a certificate template, not a proof that such a package
exists.  It also does not make its carrier ontologically fundamental.

\subsection{Branes and higher carriers}

Higher-dimensional carriers can be included by replacing $K$ with a
$p$-dimensional object and giving the corresponding action, gauge symmetries,
charges, anomalies, and dualities.  The loop-minimality theorem does not
exclude them.  It says only that dimension one is the smallest possible
continuous carrier dimension under a nonconstant-loop requirement.  It does
not order physical models by plausibility.

\subsection{A useful but limited interpretation}

String-like frameworks are natural \emph{examples} to test against a rich
saturation contract because they combine extended carriers, gauge data,
gravity-sensitive consistency conditions, anomaly equations, and explicit
dualities.  The logical direction is
\[
 \text{complete string package}
 \Longrightarrow
 \text{candidate saturated realization},
\]
after the map and indecomposability test are proved.  The reverse implication
is invalid.

This direction preserves the useful connection to string theory without
turning it into a definition.  Established string constructions provide rich
packages that may satisfy an MTT contract.  The MTT contract can then compare
which rows are shared, which are stronger, and which remain open.  It cannot
replace the worldsheet action, quantum measure, anomaly calculation, or
duality map by declaring the finished package saturated.

\section{Existence Is Not Physical Selection}

\subsection{Four different claims}

For clarity, the following statements must remain separate:
\begin{description}[leftmargin=3.3cm,style=nextline]
\item[Existence.]
$\Sol_{\Contract}\ne\varnothing$.

\item[Local rigidity.]
A point is isolated in the declared quotient topology.

\item[Class-relative uniqueness.]
Exactly one equivalence class survives in the fully enumerated candidate
class.

\item[Physical selection.]
A common source law, initial/boundary condition, or independently justified
selection functional chooses that class and yields observables.
\end{description}

Empirical adequacy is a fifth statement: the resulting observables must agree
with held-out measurements with a convention map and uncertainty budget.

\begin{proposition}[No selection from existence alone]
If a contract has two inequivalent complete saturated realizations and no
selection functional or source law distinguishing them, neither is physically
selected by the contract.
\end{proposition}

\begin{proof}
Both realizations satisfy exactly the predicates encoded by the contract.
Any rule choosing one must therefore use information not present in those
predicates.  Without such additional data, the choice is underdetermined.
\end{proof}

This remains true when each point is locally rigid.  Local isolation does not
compare separated components.

\section{Current Theorem Ledger}

\subsection{Results proved in this revision}

\begin{enumerate}
\item Saturation is a contract-relative conjunction of completeness and
indecomposability.
\item The connected components of the derivative-incidence graph exactly
detect first-order block factorization relative to declared finite blocks.
\item Connected incidence plus injective combined derivative gives local
rigidity and infinitesimal indecomposability.
\item Continuous nonconstant-loop support requires carrier dimension at least
one, and $S^1$ makes the bound sharp.
\item A nonzero analytic anomaly defect has isolated dimension zeros.
\item Quotienting by a duality identifies a duality only after that duality has
been constructed.
\item Explicit countermodels show that relative saturation alone forces none
of extended carriers, critical dimensions, worldsheets, anomalies, or
nontrivial dualities.
\end{enumerate}

\subsection{Conditional realization statements}

\begin{enumerate}
\item A one-dimensional carrier is minimal only for a declared continuous-loop
requirement.
\item A worldsheet-like sweep follows from a one-dimensional carrier plus an
independent continuation parameter; a physical worldsheet requires the
remaining action and quantum rows.
\item Critical dimensions follow only from an explicit nontrivial anomaly or
central-charge equation.
\item A string-like package realizes saturation only after all contract maps,
equations, anomalies, domains, and the indecomposability test are supplied.
\end{enumerate}

\subsection{Claims not proved}

This paper does not prove:
\begin{itemize}
\item that circle, lens, and nil exhaust all obstruction types;
\item that unified descriptions must be saturated;
\item that saturated descriptions must be extended or string-like;
\item that one-dimensional carriers are physically preferred;
\item that a numerical critical dimension is selected;
\item that anomaly cancellation or duality follows automatically;
\item that the q=79 finite branch is a complete string vacuum;
\item that a physical branch is uniquely selected; or
\item that an all-scale quantum-gravity theory has been constructed.
\end{itemize}

\section{A Reusable Saturation Audit}

A future paper claiming a saturated or unified encoding should provide the
following finite audit.

\begin{enumerate}
\item \textbf{Candidate class:} objects, morphisms, regularity, topology, and
boundary conditions.
\item \textbf{Equivalence:} every quotient or gauge identification.
\item \textbf{Constraint inventory:} explicit maps, targets, and source data.
\item \textbf{Factorization class:} which decompositions count as separate
layers.
\item \textbf{Local test:} blocked derivative, incidence graph, rank, and
kernel after quotienting.
\item \textbf{Global test:} components, nonlinear changes of variables, and
global decompositions.
\item \textbf{Carrier test:} topology and the exact property requiring
extension.
\item \textbf{Anomaly test:} fields, anomaly classes, mechanisms, and checked
trivializations.
\item \textbf{Dimension test:} the actual equation whose zero set is claimed
to be critical.
\item \textbf{Duality test:} maps, inverses, domains, dynamics, and observables.
\item \textbf{Selection test:} source law or independent selector.
\item \textbf{Empirical test:} observables, conventions, uncertainties, and
held-out comparisons.
\end{enumerate}

Failure of one row identifies the missing theorem.  It does not invalidate
the rows already proved.

\section{Conclusion}

The useful content of saturation is not that maximal consistency mysteriously
creates strings.  It is that a declared family of constraints can become
genuinely coupled and nonfactorizable.  The derivative-incidence graph gives a
finite exact test of that coupling at first order, and the rank criterion from
Program B4 can simultaneously establish local isolation.

Extended carriers, worldsheets, anomaly equations, critical dimensions, and
dualities remain powerful realization mechanisms.  Their hypotheses must be
shown, not hidden inside the word saturation.  Likewise, the existence or
rigidity of one realization does not select it physically.  This corrected
formulation preserves the unification program while turning its strongest
claims into explicit mathematical obligations.

\begin{thebibliography}{99}

\bibitem{A0}
P.~Nero,
\emph{The Modal Triplet Theory Program A0: Typed Reduction, Admissibility,
and Conditional Reconstruction},
MTT paper series, version~2, 2026.

\bibitem{B0}
P.~Nero,
\emph{The Modal Triplet Theory Program B0: Obstruction Profiles and
Conditional Encoding Responses},
MTT paper series, version~2, 2026.

\bibitem{B1}
P.~Nero,
\emph{The Modal Triplet Theory Program B1: Conditional Geometric
Reconstruction and the Gravity Encoding},
MTT paper series, version~2, 2026.

\bibitem{B2}
P.~Nero,
\emph{The Modal Triplet Theory Program B2: Conditional Gauge Reconstruction
from Principal-Bundle Data},
MTT paper series, version~2, 2026.

\bibitem{B3}
P.~Nero,
\emph{The Modal Triplet Theory Program B3: Discrete Survivor Filters and
Conditional Quantum Reconstruction},
MTT paper series, version~2, 2026.

\bibitem{B4}
P.~Nero,
\emph{The Modal Triplet Theory Program B4: Typed Encoding Intersections,
Conditional Rigidity, and Standard Model Compatibility},
MTT paper series, version~2, 2026.

\bibitem{Q79}
P.~Nero,
\emph{Consolidated Exact $\mathbb Z_{64}$ to $q=79$ Closure Theorem},
current selected result packet, SHA-256
prefix \texttt{ccacd5227f91ab08},
2026.

\bibitem{Hatcher}
A.~Hatcher,
\emph{Algebraic Topology},
Cambridge University Press, 2002.

\bibitem{KrantzParks}
S.~G.~Krantz and H.~R.~Parks,
\emph{A Primer of Real Analytic Functions},
second edition, Birkhauser, 2002.

\bibitem{AlvarezGaumeWitten}
L.~Alvarez-Gaume and E.~Witten,
Gravitational anomalies,
\emph{Nuclear Physics B} \textbf{234} (1984), 269--330.

\bibitem{GreenSchwarz}
M.~B.~Green and J.~H.~Schwarz,
Anomaly cancellations in supersymmetric $D=10$ gauge theory and superstring
theory,
\emph{Physics Letters B} \textbf{149} (1984), 117--122.

\bibitem{Buscher}
T.~H.~Buscher,
Path-integral derivation of quantum duality in nonlinear sigma-models,
\emph{Physics Letters B} \textbf{201} (1988), 466--472.

\bibitem{Polchinski}
J.~Polchinski,
\emph{String Theory, Volume I: An Introduction to the Bosonic String},
Cambridge University Press, 1998.

\end{thebibliography}

% BEGIN MTT MANAGED COMPUTATIONAL EVIDENCE
\section*{Computational Evidence and Reproducibility}
The finite arithmetic claim used by this paper is archived in the
\href{https://github.com/PeterNero/mtt-results-repro/tree/31247ebb5c22f3fbb5443024365433c6ee0bff4a}
{curated results repository at commit \texttt{31247ebb5c22}}.
The mapped authority/result identifier is
\nolinkurl{A11/q79_exact_theorem}.  It is an exact result on the selected
finite arithmetic and charge branch.  It does not supply a critical
dimension, a completed worldsheet, physical Hull--Strominger endpoints, or a
selection theorem for a saturated universe.
% END MTT MANAGED COMPUTATIONAL EVIDENCE

\end{document}
