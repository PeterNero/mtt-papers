\documentclass[11pt,a4paper,twoside]{article}

\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amsthm,amssymb,mathtools}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage[nameinlink,capitalize]{cleveref}

\setlist{leftmargin=1.3em,itemsep=0.25em,topsep=0.35em}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\CC}{\mathbb{C}}
\newcommand{\RR}{\mathbb{R}}
\newcommand{\HH}{\mathbb{H}}
\newcommand{\AF}{\mathcal{A}_{F}}
\newcommand{\AQ}{\mathcal{A}_{Q}}
\newcommand{\HF}{\mathcal{H}_{F}}
\newcommand{\DF}{D_{F}}
\newcommand{\CN}{\mathbb{C}_{N}}
\newcommand{\Tr}{\operatorname{Tr}}
\newcommand{\rank}{\operatorname{rank}}
\newcommand{\id}{\mathbf{1}}

\title{\Large\bf From Modal Triplet Theory to Noncommutative Geometry:
A Typed Almost-Commutative Encoding and Its Current Finite-Triple Frontier}
\author{Peter Nero}
\date{July 2026, Version 5}

\begin{document}
\maketitle

\begin{abstract}
We analyze the relation between Modal Triplet Theory (MTT) and
noncommutative geometry (NCG).  A coherent fixed-point projector does not by
itself derive a spectral triple, the Standard Model finite algebra, a Wick
rotation, or the spectral action.  We prove the precise compression theorem
that is available when a projector reduces an already specified real even
spectral triple.  We then record the stronger finite results now available in
the selected MTT branch.  The family factor is separated from the rank
$1<2<3$ gauge carrier; an explicit three-family chiral representation and the
faithful group
\[
  (SU(3)\times SU(2)\times U(1)_Y)/\mathbb{Z}_6
\]
are obtained; and the finite real-even triple is completed at profile tier by
the additional neutral complex summand $\CN$.  Direct enumeration of all
$676$ finite one-form basis pairs gives a raw rank-twelve scalar space,
consisting of three Higgs-doublet modules.  The selected q79/proto-spinor
alignment projector has rank four and leaves one complex Higgs doublet.  These
are finite, reproducible results, but the entries of $\DF$ remain profile
data.  The selected representation also derives the one-loop coefficient
vector $(41/10,-19/6,-7)$, and all ten matter, scalar, and gauge/ghost internal
spectrum rows are now exact finite calculations with no added continuous
parameter.  Their normalized nonzero spectral shape is universal, however,
so they do not generate the missing nonuniversal gauge thresholds.  The
four-dimensional compact Riemannian spin triple, Wick dictionary, cutoff
function, and spectral moments are not derived by the fixed-point
construction.  Moreover, the selected pure-Standard-Model running branch
rules out one universal gauge normalization: the best common-scale coupling
ratio remains $1.046656046$.  A positive overlap kinetic metric reconstructs
the profile couplings exactly but uses two measured relative coordinates.
Thus MTT currently supplies a typed almost-commutative profile encoding with
an executed finite factor, not a first-principles derivation of NCG or a
no-knob derivation of the Standard Model.
\end{abstract}

\section*{Version 5 Revision Note}
\addcontentsline{toc}{section}{Version 5 Revision Note}
\begin{description}
\item[Supersedes]
\emph{From Modal Triplet Theory to Noncommutative
Geometry: Spectral Triples, Spectral Action, and the Standard Model
Embedding}, corrected version~4.

\item[Reason]
Version~4 correctly replaced the original first-principles claims by a typed
finite profile encoding, but it predated the completed gauge-fixed
fluctuation complex and the exact ten-row internal spectrum calculation.

\item[Resolution]
Version~5 retains the A44--A52 correction and adds the verified A57--A62
finite gauge/ghost chain.  It records the exact representation-theoretic
one-loop coefficients and all ten exact internal spectrum rows, while also
recording the resulting no-go for obtaining nonuniversal gauge thresholds
from their common normalized shape.

\item[Retained result]
Given the declared Euclidean spin triple and the
selected finite profile triple, standard almost-commutative NCG reconstructs
the corresponding bosonic operator content.

\item[Remaining boundary]
A physical Lorentzian-to-Euclidean dictionary,
source-derived $\DF$ values, a selected proper-time measure and overlap
metric, a physical continuum HYM threshold source, nonperturbative quantum
transport, and a same-source physical q79 continuum realization remain open.
\end{description}

\tableofcontents
\newpage

\section{Status and logical scope}

This paper concerns a relation among three different constructions:
\begin{enumerate}
\item a coherent or fixed-point projection in MTT;
\item a finite real-even spectral triple encoding the selected particle
      representation; and
\item a four-dimensional Euclidean almost-commutative product triple and its
      spectral action.
\end{enumerate}
They are not interchangeable.  In particular, a projector is not an algebra,
a spectral gap is not a Dirac operator, and a finite representation is not a
Wick rotation.

\begin{definition}[Status vocabulary]
In this paper:
\begin{itemize}
\item \emph{exact} means an algebraic identity or finite calculation verified
      from the cited packet without measured continuous values;
\item \emph{profile} means a reconstruction using an accepted Standard Model
      parameter profile;
\item \emph{imported} means a standard NCG or QFT theorem used after its
      hypotheses are supplied independently; and
\item \emph{open} means that the current MTT source does not emit the required
      object.
\end{itemize}
\end{definition}

\noindent\textbf{Exact carrier.}
The rank-$1<2<3$ carrier and weak real structure are
selected up to unitary or phase equivalence; the family factor remains
separate.

\noindent\textbf{Exact representation.}
The $48$-state family-diagonal representation,
anomaly table, gauge group, and global $\mathbb{Z}_6$ quotient are executed.

\noindent\textbf{Mixed finite triple.}
The representation and finite axioms are exact
finite calculations; physical entries of $\DF$ use the accepted profile.

\noindent\textbf{Mixed scalar sector.}
The one-form enumeration and rank statements are
finite calculations; the Yukawa traces use profile $\DF$.

\noindent\textbf{Exact finite fluctuation spectra.}
The representation-theoretic one-loop coefficient vector and all ten
matter, scalar, and gauge/ghost internal spectrum rows are exact finite
calculations.  Their universal normalized shape is also an exact obstruction
to using these rows alone as nonuniversal gauge thresholds.

\noindent\textbf{Imported/open spacetime.}
The Euclidean product triple is standard NCG
input; MTT has not derived its physical Wick dictionary.

\noindent\textbf{Open/profile normalization.}
Only certain spectral products are
identifiable; the current overlap metric uses measured relative gauge
coordinates.

\subsection*{A concrete picture of the finite construction}

A finite spectral triple may be read as a small, structured quantum-mechanical
system.  The algebra $\AF$ lists the internal observables, the Hilbert space
$\HF$ lists the fermionic states on which they act, and the matrix $\DF$
connects states of opposite chirality.  The commutator $[\DF,a]$ measures how
an internal observable $a$ changes across those connections.  Linear
combinations of terms $a[\DF,b]$ are the finite one-forms; after the standard
reality completion they are the possible scalar and gauge fluctuations of
the supplied finite geometry.

In the present construction one family has sixteen chiral particle states.
Three families give forty-eight, and adjoining the charge-conjugate states
gives the $96$-dimensional space on which the completed real-even triple acts.
For example, the weak doublet $Q=(u_L,d_L)$ carries the quaternionic
$SU(2)$ action, its three color copies carry the $M_3(\CC)$ action, and its
abelian charge comes from the selected anomaly-free complex phase.  Entries
of $\DF$ connect these left-handed states to the corresponding right-handed
singlets.  Algebraically those entries have the role of Yukawa and neutral
mass matrices; numerically they remain profile data in this paper.

This picture also explains the Higgs calculation.  Enumerating all
$a[\DF,b]$ asks which scalar bridges the finite algebra permits.  The
unrestricted completed triple permits three real rank-four modules, or three
complex weak doublets.  The selected q79/proto-spinor alignment projector
retains one rank-four module and removes the other eight real scalar
directions.  The later ten spectrum rows ask a different question: what are
the eigenvalues of the selected finite heat operators in the seven
matter/scalar and three gauge/ghost sectors?  They certify the internal finite
shape; they are not yet physical spacetime masses or continuum thresholds.

\section{The spectral-triple contract}

\begin{definition}[Real even spectral triple]
A real even spectral triple is data
\[
  (\mathcal{A},\mathcal{H},D,J,\Gamma,\pi)
\]
where $\mathcal{A}$ is a unital involutive algebra represented faithfully by
bounded operators $\pi(a)$ on the complex Hilbert space $\mathcal{H}$,
$D$ is self-adjoint with compact resolvent, and
\[
 [D,\pi(a)]\in\mathcal{B}(\mathcal{H})
 \qquad (a\in\mathcal{A}).
\]
The grading and real structure obey the declared KO-sign table, and the
order-zero and order-one conditions are
\[
 [\pi(a),J\pi(b)J^{-1}]=0,\qquad
 [[D,\pi(a)],J\pi(b)J^{-1}]=0.
\]
Orientability and Poincare duality are additional requirements; they do not
follow merely from compact resolvent and bounded commutators.
\end{definition}

This definition makes the missing data in a projection-only argument
visible.  A coherent subspace can be useful without carrying a faithful
algebra action, a self-adjoint Dirac operator, a real structure, or a
nondegenerate intersection form.

\subsection{What a fixed-point projection can preserve}

\begin{theorem}[Reducing-projector spectral-triple theorem]
\label{thm:compression}
Let $(\mathcal{A},\mathcal{H},D,J,\Gamma,\pi)$ be a real even spectral
triple and let $P$ be an orthogonal projection such that:
\begin{enumerate}
\item $P$ commutes with $\pi(a)$ for every $a\in\mathcal{A}$;
\item $P$ reduces $D$, equivalently it commutes with the spectral
      projections of $D$;
\item $P$ commutes with $J$ and $\Gamma$; and
\item the restricted representation
      $\pi_P(a)=P\pi(a)|_{P\mathcal{H}}$ is faithful.
\end{enumerate}
Then
\[
 (\mathcal{A},P\mathcal{H},D_P,J_P,\Gamma_P,\pi_P),
 \qquad D_P=D|_{P\mathcal{H}},
\]
is a real even spectral triple with the inherited KO signs and order-zero and
order-one relations.  Orientability and Poincare duality are inherited only
when the compressed Hochschild cycle and intersection pairing satisfy the
corresponding axioms.
\end{theorem}

\begin{proof}
Because $P$ reduces $D$, the restriction $D_P$ is self-adjoint.  On
$P\mathcal{H}$,
\[
 (D_P-i)^{-1}=P(D-i)^{-1}P,
\]
which is compact.  For $a\in\mathcal{A}$,
\[
 [D_P,\pi_P(a)]=P[D,\pi(a)]P,
\]
so the commutator is bounded.  Commutation of $P$ with $J$ and $\Gamma$
restricts the real and graded relations, while the order-zero and order-one
identities follow by compression.  Faithfulness is assumed explicitly.
Neither a Hochschild orientation cycle nor nondegeneracy of the finite
intersection form is implied by these operator restrictions.
\end{proof}

\begin{corollary}[Limit of fixed-point reasoning]
\label{cor:projection-limit}
A fixed-point or harmonic projector alone does not construct a spectral
triple.  It must either act on prior spectral-triple data as in
\Cref{thm:compression}, or an independent theorem must construct the algebra,
representation, Dirac domain, real structure, grading, and finite axioms on
its image.
\end{corollary}

This is the precise separation between the MTT fixed-point architecture and
the NCG encoding.  The corrected MTT Foundation supplies typed Hilbert-bundle,
projector, gap, and stability hypotheses; it does not automatically supply
the inputs of \Cref{thm:compression}.

\section{The finite MTT carrier and the algebra no-go}

\subsection{The qutrit algebra is not the Standard Model algebra}

The selected finite qutrit package is
\[
 \AQ=\CC_{\mathrm{family}}^3\otimes M_3(\CC)
    \cong M_3(\CC)\oplus M_3(\CC)\oplus M_3(\CC).
\]
The original three-summand Standard Model candidate is
\[
 \AF^{(0)}=\CC\oplus\HH\oplus M_3(\CC).
\]

\begin{proposition}[No direct algebra identification]
\label{prop:no-direct}
There is no real involutive-algebra isomorphism
$\AQ\cong\AF^{(0)}$.
\end{proposition}

\begin{proof}
The real dimensions are
\[
 \dim_{\RR}\AQ=54,\qquad
 \dim_{\RR}\AF^{(0)}=24.
\]
Their centers also have different real dimensions:
\[
 \dim_{\RR}Z(\AQ)=6,\qquad
 \dim_{\RR}Z(\AF^{(0)})=5.
\]
Either invariant rules out an isomorphism.
\end{proof}

The lane-wise corner construction of A44 is an exact abstract reduction, but
A45 corrects its physical interpretation: the $\CC^3$ label is the
family/character factor and cannot simultaneously be used as three
family-dependent gauge-rank lanes.  The physical construction therefore
keeps
\[
 \mathcal{H}_{\mathrm{chiral}}
   =\CC^3_{\mathrm{family}}\otimes\mathcal{H}_{16},
 \qquad
 \rho_{\mathrm{gauge}}
   =\id_3\otimes\rho_{16},
\]
and builds the gauge factor separately from the selected rank flag.

\subsection{Native automorphisms and the global gauge group}

On the selected rank-one, rank-two, and determinant-trivial rank-three
carriers, the automorphism groups are
\[
 U(1),\qquad USp(2)\cong SU(2),\qquad SU(3).
\]
The rank-two result uses the selected symplectic antiunitary
$J=\epsilon K$, and the rank-three result uses the determinant-trivial
carrier.  On the explicit chiral representation the diagonal central kernel
is $\mathbb{Z}_6$.  Thus the faithful low-energy group is
\[
 G_{\mathrm{SM}}
   =\frac{SU(3)\times SU(2)\times U(1)_Y}{\mathbb{Z}_6}.
\]
The associated $48$-state three-family chiral representation contains the
rows $Q,u^c,d^c,L,e^c,N^c$ and passes the local and global anomaly tests in
the A46--A47 packets \cite{MTTResultsRepro2026}.

\section{The completed finite real-even triple}

\subsection{Why the neutral summand is required}

The explicit particle-antiparticle construction has complex dimension $96$.
For the accepted charged and neutral profile matrices, the finite Dirac
operator satisfies self-adjointness, oddness, KO-dimension-six reality,
order zero, and order one.  The three-summand candidate
$\AF^{(0)}$, however, fails two remaining finite axioms:
\begin{itemize}
\item the $N_R:\CC\!-\!\CC$ self-edge obstructs the required orientation; and
\item the antisymmetric $3\times3$ intersection form has determinant zero.
\end{itemize}

The minimal selected completion is
\[
 \AF=\CC\oplus\HH\oplus M_3(\CC)\oplus\CN,
\]
where
\[
 \CN=\operatorname{End}_{\CC}(1_M),\qquad 1_M=N^c.
\]
Only the neutral edge is changed, from $\CC\!-\!\CC$ to
$\CN\!-\!\CC$.  The completion has an explicit $17$-term Hochschild
zero-cycle and intersection determinant $4$ per family, hence $324$ for the
three-family product.  No continuous parameter or new state is added.

\subsection{One physical abelian circle}

Let $(\alpha,\mu,\nu)$ denote the phases of
$\CC,M_3(\CC),\CN$.  The independent anomaly equations are
\[
 \alpha+3\mu=0,\qquad \alpha-\nu=0.
\]
Their primitive integer null vector is
\[
 (\alpha,\mu,\nu)=(3,-1,3),
\]
which gives
\[
 6Y(Q,u^c,d^c,L,e^c,N^c)
   =(1,-4,2,-3,6,0).
\]
The cubic anomaly vanishes on the same line, whereas an independent
$\CN$ phase is anomalous.  Distinct algebra sheets therefore share one
anomaly-free physical hypercharge circle.

\begin{theorem}[Current finite-triple result]
\label{thm:finite}
The A45--A50 packet chain defines an explicit three-family real-even finite
triple on the completed algebra $\AF$ at the declared profile tier.  The
representation, KO signs, order-zero and order-one identities,
orientability, Poincare pairing, anomaly-free hypercharge line, and global
$\mathbb{Z}_6$ quotient are finite executable statements.  The numerical
entries of $\DF$, including Yukawa and Majorana data, remain accepted profile
coordinates rather than source-derived MTT predictions.
\end{theorem}

\begin{proof}
The representation and anomaly table are A46; the native automorphism group
and central quotient are A47; the explicit bimodule is A48; the finite Dirac
and three-summand obstruction are A49; and the neutral completion and unique
abelian anomaly-free line are A50.  Their hash-addressed artifacts and
independent verifiers are recorded in the curated result repository
\cite{MTTResultsRepro2026}.  The profile status of the entries of $\DF$ is
part of the A49 contract and is not changed by the algebraic checks.
\end{proof}

\section{Finite one-forms and the Higgs sector}

For the completed finite triple, define
\[
 \Omega_{\DF}^{1}(\AF)
   =\operatorname{span}\{\rho(a)[\DF,\rho(b)]:a,b\in\AF\}.
\]
The A51 calculation evaluates all $26\times26=676$ real-algebra basis pairs
and then imposes self-adjoint real fluctuations
\[
 A_{\mathrm{real}}=A+J_FAJ_F^{-1}.
\]

\begin{proposition}[Raw scalar multiplicity and selected projection]
\label{prop:higgs}
The unrestricted real fluctuation space has real rank $12$ and decomposes
into three rank-four scalar-doublet modules,
\[
 H_{\mathrm{up}},\qquad
 H_{\mathrm{down/charged\ lepton}},\qquad
 H_{\mathrm{neutrino}}.
\]
Consequently the raw completed finite triple is not automatically the
one-Higgs Standard Model.  The selected q79/proto-spinor alignment rule
\[
 H_{\mathrm{up}}=H_{\mathrm{neutrino}}=H,\qquad
 H_{\mathrm{down}}=H_{\mathrm{charged\ lepton}}
   =-\epsilon\overline{H}
\]
defines a self-adjoint idempotent rank-four submodule and removes the eight
unwanted real scalar directions.
\end{proposition}

The finite execution reports inclusion residual
$6.152\times10^{-15}$ in its floating-point realization.  The surviving
module is one complex $SU(2)$ doublet of hypercharge $+1/2$, with
pseudoreality supplying its conjugate channels.  This is an executed finite
selection.  It is not yet a derivation of the same projector from the
physical nonzero-Chern q79 HYM connection.

For three families, the finite gauge traces are
\[
 k_Y:k_2:k_3=10:6:6.
\]
After the conventional $5/3$ hypercharge normalization they become
$6:6:6$.  The accepted profile $\DF$ gives
\[
 a=\Tr(Y^\dagger Y)=3.15667873398489,\qquad
 b=\Tr((Y^\dagger Y)^2)=3.31696406124945,
\]
\[
 \frac{b}{a^2}=0.332874093710992.
\]
These are profile traces at $M_Z$, not high-scale predictions.

\section{Euclidean product triple and the Wick contract}

Let $(Y_E,g_E)$ be a compact oriented four-dimensional Riemannian spin
manifold with spin triple
\[
 \bigl(C^\infty(Y_E),L^2(Y_E,S_E),D_E^{(4)},J_E,\gamma_5\bigr).
\]
Given the finite factor of \Cref{thm:finite}, the standard product data are
\[
\begin{aligned}
 \mathcal{A}&=C^\infty(Y_E)\otimes\AF,\\
 \mathcal{H}&=L^2(Y_E,S_E)\otimes\HF,\\
 D_{\mathrm{prod}}
   &=D_E^{(4)}\otimes\id+\gamma_5\otimes\DF.
\end{aligned}
\]

\begin{theorem}[Conditional product-triple theorem]
\label{thm:product}
If the Euclidean spin triple and the finite real-even triple satisfy the
standard sign, order, orientation, and nondegeneracy hypotheses, then their
graded product is an almost-commutative real even spectral triple.  Inner
fluctuations generate the associated gauge fields and finite scalar modules.
\end{theorem}

This is a standard NCG theorem
\cite{Connes1994,ConnesMarcolli2007,vanSuijlekom2015}; it is not a theorem
that MTT has selected $(Y_E,g_E)$ or its Dirac operator.

\begin{definition}[Physical Wick contract]
A physical use of \Cref{thm:product} requires, at minimum:
\begin{enumerate}
\item a Lorentzian spin background and causal field domain;
\item a declared analytic continuation to the Euclidean background;
\item maps for spinors, gauge fields, scalar fields, actions, and boundary
      conditions;
\item a contour and inverse domain controlling singularities and zero modes;
      and
\item a theorem relating Euclidean correlation functions or determinants to
      Lorentzian observables.
\end{enumerate}
\end{definition}

The spectral-action formulation is intrinsically Euclidean at this stage.
Wick rotation and the removal of unphysical fermion doubling require a
separate prescription \cite{DAndreaKurkovLizzi2016}.  A compact phase circle
or the q79 shared differential line is not physical time and does not supply
this contract automatically.

\section{Spectral action: conditional output and no-go results}

For a positive cutoff profile $f$ and scale $\Lambda$, write the bosonic
spectral action in the convention
\[
 S_B(f,\Lambda)=\Tr f(D_{\mathrm{prod}}^2/\Lambda^2).
\]
Under the usual heat-kernel hypotheses,
\[
 S_B(f,\Lambda)
 \sim \Lambda^4 f_4 a_0(D_{\mathrm{prod}}^2)
     +\Lambda^2 f_2 a_2(D_{\mathrm{prod}}^2)
     +f_0 a_4(D_{\mathrm{prod}}^2)+\cdots,
\]
where, in this convention,
\[
 f_4=\int_0^\infty f(u)u\,du,\qquad
 f_2=\int_0^\infty f(u)\,du,\qquad
 f_0=f(0).
\]
The heat-kernel theorem produces cosmological, Einstein--Hilbert,
Yang--Mills, scalar kinetic and potential, and nonminimal curvature terms
from the supplied product triple
\cite{ChamseddineConnes1997,Vassilevich2003}.

\subsection{What the finite execution closes}

Combining \Cref{thm:product} with \Cref{prop:higgs} closes the bosonic
\emph{operator content} at profile tier: three gauge terms, one Higgs
covariant kinetic term and potential, and the standard gravitational terms.
It does not fix absolute coefficients until the base geometry, cutoff data,
field normalization, and RG transport are supplied.

\subsection{Gauge-fixed heat data and the ten finite spectra}

The later finite execution sharpens this statement without changing its
tier.  From the selected $Q,u,d,L,e,N$ chiral representations, the one-Higgs
scalar module, the adjoint gauge blocks, and Faddeev--Popov ghosts, A57 derives
the standard one-loop coefficient vector
\[
  b=\left(\frac{41}{10},-\frac{19}{6},-7\right)
\]
by representation indices.  The accepted QFT vector is not used to select
this result.  Its interpretation as a physical running law still imports the
usual gauge-fixed one-loop QFT theorem.

A58--A62 then execute the seven $Q,u,d,L,e,N,H$ rows and the three
$U(1)$, $SU(2)$, and $SU(3)$ gauge/ghost rows.  The final two nonabelian rows
are
\[
\begin{array}{c|ccc}
 &0&4\pi^2/9&8\pi^2/9\\ \hline
 SU(2)&3&12&12\\
 SU(3)&8&32&32
\end{array}
\]
with the table entries giving multiplicities.  The $U(1)$ adjoint
self-interaction row vanishes, as required by $C_2(U(1))=0$.  These ten
internal spectra are exact and add no continuous scale.

This completion has a useful negative consequence.  After normalization per
representation or adjoint rank, every nontrivially weighted row has the same
finite spectral shape.  It can supply a common finite heat factor, but not the
different threshold corrections needed to repair the universal-boundary
failure below.  A nonuniversal physical threshold still requires a
same-source continuum HYM or mass-dependent operator, not a relabeling of the
finite spectra.

\subsection{Universal gauge normalization is excluded}

Equal normalized finite traces with one universal $f_0$ and identity kinetic
metric would require
\[
 g_1^{\mathrm{GUT}}(Q)=g_2(Q)=g_3(Q)
\]
at one scale.  On the selected pure-Standard-Model running branch, A52 finds
the best one-loop point
\[
 Q_{\mathrm{best}}=1.71709699661\times10^{14}\ {\rm GeV},
\]
\[
 (g_1,g_2,g_3)
   =(0.556290097,0.535101507,0.560067228),
\qquad
 \frac{\max g_i}{\min g_i}=1.046656046.
\]
The archived multi-loop trajectory has the same failure.

\begin{proposition}[Universal-normalization no-go]
\label{prop:gauge-nogo}
On the selected pure-Standard-Model profile branch, no single universal
spectral moment with identity gauge kinetic metric reproduces all three
running gauge couplings.
\end{proposition}

The exact profile reconstruction instead uses
\[
 K_{\mathrm{gauge}}
   =\operatorname{diag}
     (1.9568425763574,1,0.309837025907476),
\qquad
 f_0=0.397408225924864,
\]
in $g_i^{-2}=6f_0K_i$, with residual $4.578\times10^{-16}$.  Its two
relative entries are the two measured relative coupling coordinates.  They
add no parameters beyond the Standard Model profile but are not MTT
predictions.

\subsection{Moment identifiability}

The displayed action determines only
\[
 f_0K_i,\qquad f_2\Lambda^2,\qquad f_4\Lambda^4.
\]

\begin{proposition}[Spectral-moment identifiability]
\label{prop:moments}
The spectral action above cannot separately determine
$\Lambda,f_2,f_4$.
\end{proposition}

\begin{proof}
For every $c>0$, the transformation
\[
 \Lambda\mapsto c\Lambda,\qquad
 f_2\mapsto f_2/c^2,\qquad
 f_4\mapsto f_4/c^4
\]
leaves $f_2\Lambda^2$ and $f_4\Lambda^4$ unchanged.
\end{proof}

The former claim that a fixed-point gap alone fixes the cutoff function,
spectral moments, Newton normalization, and vacuum term is therefore
withdrawn.  A selected proper-time measure and source-derived overlap metric
would be needed for that stronger result.

\section{Relation to q79, fixed points, and quantum field theory}

\subsection{The shared circle}

The finite triple has distinct complex algebra sheets but one anomaly-free
hypercharge circle.  The corrected Foundation and Proto-Spinor papers also
identify a universal flat q79 differential line and finite carrier actions.
The equality of these roles is not automatic.  A physical identification
must preserve the selected connections, covariant derivatives, Hessians, and
holonomies.  The nonzero-Chern HYM realization and its local
strain-to-q79 continuum intertwiner remain open.

\subsection{Renormalization}

The A03 observable functor recovers perturbative Standard Model observables
after importing the standard gauge-fixed renormalized action and
BRST/Faddeev--Popov quantization.  That result is compatible with the finite
NCG encoding but does not derive quantization from the spectral triple.
Conversely, the Euclidean spectral action does not establish a
nonperturbative Lorentzian QFT, a quantum BV pushforward, or ultraviolet
completeness.  The v3 claim that FRG, pAQFT, and the MTT quantum-gravity
program ``match'' from the common vector $\Theta$ is therefore replaced by
this typed compatibility statement.

\section{Tiered synthesis theorem}

\begin{theorem}[Selected MTT--NCG encoding theorem]
\label{thm:synthesis}
Assume:
\begin{enumerate}
\item the exact selected finite carrier, chiral representation, native gauge
      automorphisms, neutral completion, and anomaly-free circle of A45--A50;
\item the accepted profile $\DF$ and the selected rank-four scalar projector
      executed in A51;
\item an independently supplied compact four-dimensional Euclidean spin
      triple; and
\item a cutoff profile, scale, field normalization, and RG convention.
\end{enumerate}
Then the standard almost-commutative product construction gives a real even
spectral triple and reproduces the bosonic Standard Model operator content
with one Higgs doublet at the declared profile tier.

The conclusion does not derive:
\begin{enumerate}
\item the physical Lorentzian spacetime or its Wick rotation;
\item the accepted entries of $\DF$ from source geometry;
\item a universal spectral normalization on the pure-SM branch;
\item a unique cutoff function, scale, or spectral moments;
\item a no-knob Standard Model parameter point; or
\item the renormalized quantum theory or ultraviolet completion.
\end{enumerate}
\end{theorem}

\begin{proof}
The finite premises give \Cref{thm:finite} and
\Cref{prop:higgs}.  The imported Euclidean premise gives
\Cref{thm:product}; the standard heat-kernel expansion then gives the
bosonic operator content.  The six exclusions follow respectively from the
missing Wick contract, the profile status of $\DF$, the no-go in
\Cref{prop:gauge-nogo}, the degeneracy in \Cref{prop:moments}, the open
source-value program, and the absence of a quantum transport theorem.
\end{proof}

\section{Reproducibility and remaining frontier}

The finite results used here are archived in the curated calculation
repository \cite{MTTResultsRepro2026}.  The principal result identifiers are:
\begin{itemize}
\item \textbf{A46:} \nolinkurl{typed_family_representation};
      the $48$-state representation and anomaly table.
\item \textbf{A47:} \nolinkurl{native_gauge_group};
      native $U(1)$, $SU(2)$, $SU(3)$ automorphisms and the
      $\mathbb{Z}_6$ quotient.
\item \textbf{A49:} \nolinkurl{physical_df_96};
      the explicit profile $96\times96$ finite Dirac operator and finite-axiom
      tests.
\item \textbf{A50:} \nolinkurl{neutral_summand_hypercharge};
      the neutral completion and unique anomaly-free hypercharge line.
\item \textbf{A51:} \nolinkurl{finite_inner_fluctuation};
      the $676$-pair one-form execution, selected one-Higgs projector, and
      finite traces.
\item \textbf{A57:} \nolinkurl{gauge_fixed_fluctuation_complex};
      the selected gauge-fixed complex and exact one-loop coefficient vector.
\item \textbf{A62:} \nolinkurl{ten_spectrum_closure};
      completion of all ten exact finite internal spectrum rows and the
      universal-shape limitation.
\end{itemize}

The next strict mathematical exits are:
\begin{enumerate}
\item construct the physical nonpullback q79 HYM endpoints and the
      connection-preserving continuum intertwiner;
\item derive the entries of $\DF$ and the overlap kinetic metric without
      measured profile coordinates;
\item derive a sector-dependent physical threshold operator; the completed
      finite spectra alone cannot provide it;
\item select a proper-time measure and determine the identifiable spectral
      products from source geometry;
\item supply the Lorentzian/Wick and physical-state contracts; and
\item complete renormalized quantum transport with an uncertainty and
      observable-comparison theorem.
\end{enumerate}

\section{Conclusion}

The corrected result is narrower than v3 and materially stronger than a
disclaimer.  MTT now has an executed finite almost-commutative profile
encoding: the family and gauge carriers are correctly typed, the finite
real-even triple is completed, the anomaly-free circle and global gauge group
are explicit, and the finite one-form calculation explains why a selected
projector is needed to obtain one Higgs doublet.  The gauge-fixed complex now
also derives the one-loop representation coefficients and closes all ten
finite internal spectrum rows.  The same analysis identifies exact
obstructions: the qutrit algebra cannot simply be renamed as the Standard
Model algebra, the uncompleted three-summand finite triple fails orientation
and duality, the raw fluctuation space has three doublets, the normalized
finite spectra cannot generate nonuniversal thresholds, and a single
universal spectral normalization fails on the selected pure-SM running
branch.  What remains is not ``derive NCG again.''  It is to source the
Euclidean/Lorentzian bridge, finite values, physical threshold operator,
overlap metric, and spectral moments from one physical MTT geometry.

\bibliographystyle{plain}
\bibliography{main}

\end{document}
